'This script is typed in to an editor'
# It doesn't run as you type it.
# But, at least it gets cool colors.

n = 5
pad_char = '_'       # Put a space between the quotes

for i in range(n):   # Orange words are keyswords
    pad = pad_char * i
    print(pad, 'Wow, it moves to the right!!!')

print('Done. Fini.  But not Kaput.')   # Purple words are builtin funcs


